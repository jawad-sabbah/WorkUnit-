<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
  <head>
    <meta charset="UTF-8" />
    <title></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="h-full">

    <div class="min-h-full flex">
      
      <!-- Sidebar -->
      <aside class="hidden md:flex md:flex-col w-64 bg-white border-r shadow-sm">
        <div class="h-16 flex items-center justify-center border-b">
          <h1 class="text-xl font-bold text-blue-600">CollabSpace</h1>
        </div>
        <nav class="flex-1 p-4 space-y-2">
          <% if (user) { %>
            <a href="/dashboard" class="block text-gray-700 hover:text-blue-600">🏠 Dashboard</a>
            <a href="/projects" class="block text-gray-700 hover:text-blue-600">📁 Projects</a>
            <a href="/profile" class="block text-gray-700 hover:text-blue-600">👤 Profile</a>
            <a href="/auth/logout" class="block text-red-500 hover:underline">🚪 Logout</a>
          <% } else { %>
            <a href="/auth/login" class="block text-blue-600 font-medium">Log In</a>
            <a href="/auth/register" class="block text-blue-600 font-medium">Register</a>
          <% } %>
        </nav>
      </aside>

      <!-- Main content area -->
      <div class="flex-1 flex flex-col">
        
        <!-- Top navbar -->
        <header class="h-16 bg-white border-b px-4 flex items-center justify-between shadow-sm">
          <div class="md:hidden">
            <!-- Mobile menu button (optional) -->
          </div>
          <h2 class="text-lg font-semibold text-gray-700">Welcome</h2>
          <% if (user) { %>
            <div class="text-sm text-gray-600">
              Hello, <span class="font-medium"><%= user.username %></span>
            </div>
          <% } %>
        </header>

        <!-- Page content -->
        <main class="flex-1 overflow-y-auto p-6 bg-gray-50">
          
        </main>

        <!-- Footer -->
        <footer class="bg-white text-center text-gray-400 text-sm py-4 border-t">
          &copy; 2025 CollabSpace. All rights reserved.
        </footer>
      </div>
    </div>

  </body>
</html>
